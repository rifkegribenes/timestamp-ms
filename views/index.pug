doctype html
html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title #{title} - Timestamp Microservice
    link(id='favicon', rel='icon', type='image/x-icon', href='')
    link(href='https://fonts.googleapis.com/css?family=Open+Sans', rel='stylesheet')
    link(href='/public/style.css', rel='stylesheet')
    script(src='/public/script.js') 

  body
    header(class='header')
      h1 Timestamp Microservice
      h2 FreeCodeCamp Back End Certification API project #1
      
    main(class='container')
      h3 User stories:
      blockquote I can pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp or a natural language date (example: January 1, 2016)
      blockquote If it does, it returns both the Unix timestamp and the natural language form of that date.
      blockquote If it does not contain a date or Unix timestamp, it returns null for those properties.
      div(class='row')
        div(class='col')
          div(class='card')
            div(class='card__title') Example Usage
              p(id='natural-link')
              p(id='unix-link')
            div(id='try' class='card__action')
        div(class='col')
          div(class='card')
            div(class='card__title') Example Output
            pre { unix: 1450117800, natural: "December 15, 2015" }
            
    footer(class='footer')
      div(class='icon__wrap')
        a(class='github' href='https://github.com/rifkegribenes/timestamp-ms')
          img(src='' alt='view code on github')
          
      